// ignore_for_file: constant_identifier_names

import 'package:flutter/widgets.dart';

ValueNotifier<bool> showSolde = ValueNotifier(false);

var prefs, spendingsBox;

class Boxes {
  static String spendings = "spendings";
}
